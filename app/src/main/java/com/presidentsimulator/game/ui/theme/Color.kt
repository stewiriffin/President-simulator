package com.presidentsimulator.game.ui.theme

import androidx.compose.ui.graphics.Color

/** Primary war-room background (#0F172A). */
val DeepNavy = Color(0xFF0F172A)

/** Elevated navy for surfaces and cards. */
val NavySurface = Color(0xFF1E293B)

/** Slate panels and secondary surfaces. */
val SlateGray = Color(0xFF334155)

/** Outline / divider slate. */
val SlateOutline = Color(0xFF475569)

/** High-contrast body text. */
val StarkWhite = Color(0xFFF8FAFC)

/** Secondary labels and captions. */
val NeutralGray = Color(0xFF94A3B8)

/** Accent for primary actions. */
val CommandGold = Color(0xFFE2B659)

/** Semantic: surplus / profit (#10B981). */
val ProfitGreen = Color(0xFF10B981)

/** Semantic: deficit / loss (#EF4444). */
val DeficitRed = Color(0xFFEF4444)

/** Semantic: caution / crisis titles. */
val WarningOrange = Color(0xFFF59E0B)

/** Semantic: informational metrics. */
val InfoBlue = Color(0xFF38BDF8)

/** Critical alert container. */
val CrisisCrimson = Color(0xFF7F1D1D)
